package cal.ose.internose.service;

import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import static org.mockito.Mockito.*;

public class EmployeurServiceTest {


    /*@Mock
    private EmployeurRepository employeurRepository;

    @InjectMocks
    private EmployeurService employeurService;

    @Test
    void testCreerEmployeur(){
        String prenom = "John123";
        String nom = "Doe123";
        String email = "JohnDoe@123";
        String entreprise = "Entreprise123";

        Employeur employeur = Employeur.builder()
                .prenom(prenom)
                .nom(nom)
                .email(email)
                .entreprise(entreprise)
                .build();

        when(employeurRepository.save(any(Employeur.class))).thenReturn(employeur);

        employeurService.creerEmployeur(new EmployeurDTO(prenom, nom, email, entreprise));

    }*/
}
